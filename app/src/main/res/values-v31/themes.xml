<resources>
    <!-- Base application theme. -->
    <style name="Theme.Ce_que_vous_voulez" parent="BaseTheme">
        <!-- splash screen -->
        <item name="android:windowSplashScreenBackground">@color/black</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground
        </item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/white</item>
    </style>
</resources>